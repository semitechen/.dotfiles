# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Stow backup files
*.stowbak

# Neovim swap/undo files (though they are usually in ~/.local/state)
*.swp
*.swo

# Gemini CLI transient files
gemini/.gemini/history/
gemini/.gemini/tmp/
gemini/.gemini/state.json
gemini/.gemini/installation_id

# Tmux plugins (if you prefer to install them via TPM on new machine)
# tmux/.config/tmux/plugins/

spotatui/.config/spotatui/.spotify_token_cache_*.json
spotatui/.config/spotatui/streaming_cache/
